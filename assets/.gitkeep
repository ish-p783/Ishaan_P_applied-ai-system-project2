# This folder holds system architecture images (e.g. exported PNGs of the
# Mermaid diagrams in ../diagrams/). Kept in git with this placeholder so the
# empty folder is tracked. Replace/remove once you add real image files.
